% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assemble_psofa_data.R
\name{assemble_psofa_data}
\alias{assemble_psofa_data}
\title{assemble_psofa_data}
\usage{
assemble_psofa_data(
  labs,
  vitals,
  meds,
  fio2_spo2,
  resp_episodes,
  time_window,
  t_min = 0,
  t_max = 24,
  agem = NULL,
  dob = NULL,
  fio2_spo2_key_col = "PAT_ENC_CSN_ID",
  fio2_spo2_time_col = "RECORDED_TIME",
  fio2_spo2_var_col = "DISPLAY_NAME",
  fio2_spo2_measure_col = "MEASURE_VALUE",
  fio2_varname = "FiO2 (\%)",
  spo2_varname = "SpO2"
)
}
\arguments{
\item{labs}{Path to a pipe-delimited labs file, or a pre-loaded data frame
in the format returned by \link{load_labs}.}

\item{vitals}{Path to a pipe-delimited vitals file, or a pre-loaded data
frame in the format returned by \link{load_vitals}.}

\item{meds}{Path to a pipe-delimited MAR file, or a pre-loaded data frame
in the format returned by \link{load_meds}.}

\item{fio2_spo2}{Path to a pipe-delimited flowsheet file containing FiO2
and SpO2 rows, or a pre-loaded data frame with columns \code{enc_id},
\code{recorded_time}, \code{fio2}, and \code{spo2}.}

\item{resp_episodes}{A pre-computed data frame of respiratory support
episodes, as returned by \link{classify_resp_support}.}

\item{time_window}{A data frame with column \code{enc_id} and either:
\itemize{
\item \code{t_start} and \code{t_end} (POSIXct) for absolute mode, or
\item \code{ref_time} (POSIXct) for relative mode, with offsets
supplied via \code{t_min} and \code{t_max}.
}}

\item{t_min}{Numeric. Hours after \code{ref_time} defining the start of the
scoring window (relative mode only). Default \code{0}.}

\item{t_max}{Numeric. Hours after \code{ref_time} defining the end of the
scoring window (relative mode only). Default \code{24}.}

\item{agem}{Optional data frame with columns \code{enc_id} and \code{agem}
(age in months). Takes priority over \code{dob} if both are supplied.}

\item{dob}{Optional data frame with columns \code{enc_id} and \code{dob}
(date of birth as Date or character in YYYY-MM-DD format). Age is computed
from \code{dob} and the window start time. Ignored if \code{agem} is
supplied.}

\item{fio2_spo2_key_col}{Name of the encounter ID column in the FiO2/SpO2
flowsheet file. Default \code{'PAT_ENC_CSN_ID'}.}

\item{fio2_spo2_time_col}{Name of the timestamp column. Default
\code{'RECORDED_TIME'}.}

\item{fio2_spo2_var_col}{Name of the variable name column. Default
\code{'DISPLAY_NAME'}.}

\item{fio2_spo2_measure_col}{Name of the measurement column. Default
\code{'MEASURE_VALUE'}.}

\item{fio2_varname}{Display name used for FiO2 rows in the flowsheet.
Default \code{'FiO2 (\%)'}.}

\item{spo2_varname}{Display name used for SpO2 rows in the flowsheet.
Default \code{'SpO2'}.}
}
\value{
A wide-format data frame with one row per encounter and columns:
\code{enc_id}, \code{agem}, \code{platelets}, \code{tbili},
\code{creatinine}, \code{pf_ratio}, \code{sf_ratio}, \code{map},
\code{epi}, \code{norepi}, \code{dopa}, \code{dobut},
\code{resp_support}.
}
\description{
Assemble a wide-format data frame containing the worst physiologic values
within a specified time window, suitable for pSOFA scoring via
\link{calc_psofa}.
}
